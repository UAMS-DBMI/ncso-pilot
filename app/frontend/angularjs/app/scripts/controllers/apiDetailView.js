'use strict';

angular.module('ncsoDemo')
    .controller('APIDetailView', function ($scope) {
      // TODO: grab <apiname> from url path param and get the output of the api and shove into the view

    });